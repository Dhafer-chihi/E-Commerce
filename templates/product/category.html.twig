{% extends "base.html.twig" %}
{% block title  %}
      categorie:{{slug}}
{% endblock title %}
{% block body %}
<ol class="breadcrumb">
    <li class="breadcrumb-item"><a href="{{path('homepage')}}">Acceuil</a></li>
    <li class="breadcrumb-item">{{category.name}}</li>
  </ol>
  <br>
  <h2>Categorie : {{category.name}} / {{category.slug}}</h2>
  <div class="row">
    {% for p in category.products %}
    <div class="col-4">
       {% include "product/_product_card.html.twig" %}
    </div>  
      {% endfor %}
  </div>
      

{% endblock body %}